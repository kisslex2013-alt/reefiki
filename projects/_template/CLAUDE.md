# Claude Code — Project: NAME

Полный контракт проекта: **`AGENTS.md`** в этой же папке. Прочитай его — там операции, принципы, триггеры, безопасность, стиль общения.

Этот файл — только Claude-specific дополнение.

## Slash-команды

`/save` `/process` `/query` `/harvest` `/lint` `/status` `/reindex` `/resolve` `/help`. Полные спецификации — в `.claude/commands/<op>.md`.
