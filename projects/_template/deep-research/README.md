# Deep research

Use this folder for source-heavy investigations that are too large or too raw for one wiki page.

Create a new topic by copying `_template/` to `deep-research/<topic-slug>/`.

Close a topic only after the reusable conclusion is distilled into `wiki/` or explicitly rejected into `seen/`.

